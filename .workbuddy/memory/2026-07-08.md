## 2026-07-08

### Starry 个人网站 — 全部 5 个 Phase 完成 (27/27)

- 初始化 Next.js 16 + TypeScript + Tailwind CSS 4，位于 `D:/workstation/blog`
- Turbopack 在本地有 TLS 网络问题，dev/build 脚本已改为 `--webpack` 模式
- 全部 8 个公开页面：首页/文章/项目/时间线/关于/简历 + 文章详情/项目详情
- 中文化完整，纯中性色设计，系统字体栈，响应式布局
- 文件驱动架构：内容即 `src/content/posts/*.md` （Obsidian 可直接编辑）
- 管理后台 `/admin/login`（密码 starry2026）：文章CRUD + 项目CRUD + 时间线
- Markdown 编辑器：textarea + 右侧实时预览
- SEO：所有页面 metadata + sitemap.xml + robots.txt
- 可访问性：skip-to-content + focus-visible + aria-labels
- 404/500 自定义错误页
- 零外部依赖：已移除 Supabase，数据层直接读 MD 文件
- git：已初始化并完成首次 commit (c5c51d5)

### 添加后台快捷入口按钮 (19:22)
- 新增 `AdminFab` 组件：右下角可拖动圆形浮动按钮（⚙），点击跳转 `/admin/settings`
- 在 `/admin` 路径下自动隐藏
- 已加入根布局

### 修复 AdminFab 导致首页崩溃 (20:00)
- 问题：`usePathname()` 在静态生成/客户端初始化期间可能返回 `null`，直接调用 `.startsWith()` 抛 TypeError
- 解决方案：移除 `usePathname`，改用 `useEffect` 中读取 `window.location.pathname`
- 回滚并重新部署验证

### 下一步：Vercel 部署上线
