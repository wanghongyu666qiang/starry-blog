## 2026-07-08

### Starry 个人网站 — Phase 1 完成

- 初始化 Next.js 16 + TypeScript + Tailwind CSS 4，位于 `D:/workstation/blog`
- Turbopack 在本地有 TLS 网络问题，dev/build 脚本已改为 `--webpack` 模式
- 技术栈确认：Next.js (webpack) + React 19 + Tailwind 4 + Supabase JS
- 设计决策：
  - 纯中性色（无强调色），靠排版建立层级
  - 系统字体栈，零外部依赖
  - 简易 textarea + 右侧预览面板做 Markdown 编辑器
  - Timeline 最终用纯 CSS 实现（比任何库都轻量）
  - Resume PDF 导出暂缓
- 目录结构：`src/components/`, `src/lib/`, `src/types/`, `src/content/`
- 组件：Header（导航栏）+ Footer + Layout
- Supabase 客户端初始化 + 类型定义 + schema.sql（posts/projects/timeline/settings/profile 五张表）

### Starry 个人网站 — Phase 2 完成

- 全部 8 个公开页面已构建，全部返回 HTTP 200
- 所有页面已中文化
- 导航栏：移动端换行布局（flex-wrap），无滚动条
- 响应式：md 断点 + flex-wrap + TOC 桌面侧栏/移动隐藏

### Starry 个人网站 — Phase 3 完成

- Markdown 渲染管线：react-markdown + remark-gfm + remark-math + rehype-katex + rehype-highlight
- 代码高亮：rehype-highlight (github.css 主题)，同步渲染兼容 SSR/SSG
- KaTeX 数学公式：$$ 块级 + $ 行内公式
- Mermaid：客户端动态导入，useEffect 渲染
- 数据访问层：Supabase 查询 + 静态数据 fallback（未配置 Supabase 时自动降级）
- 所有页面已接入数据层，不再内嵌硬编码数据
- 文章详情页已展示真实 Markdown 渲染效果

### 下一步：Phase 4 管理后台（Supabase Auth + CRUD + 编辑器）
