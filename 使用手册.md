# Starry Blog 使用手册

## 1. 站点说明

Starry Blog 是一个基于 Next.js 的个人品牌站，主要展示技术文章、项目作品、个人介绍和简历。线上地址为：

```text
https://www.starrylovetbao.cloud
```

站点没有网页管理后台，文章和项目均通过仓库内的 Markdown 文件维护。当前不再提供时间线页面或时间线数据文件。

## 2. 本地启动

项目的 CI 和 Vercel 生产环境使用 Node.js 24。首次获取代码后，在项目根目录执行：

```powershell
npm.cmd ci
npm.cmd run dev
```

打开 `http://localhost:3000` 即可预览。项目在 Windows 上固定使用 Webpack 启动 Next.js，无需额外传入参数。

日常使用的命令：

```powershell
npm.cmd run dev               # 启动本地开发服务器
npm.cmd run content:validate  # 校验文章和项目内容
npm.cmd run lint              # 执行 ESLint
npm.cmd run typecheck         # 执行 TypeScript 检查
npm.cmd test                  # 运行全部测试
npm.cmd run build             # 生成生产构建
npm.cmd start                 # 启动已完成构建的生产服务器
npm.cmd run check             # 按发布顺序执行全部检查
```

## 3. 页面入口

| 路径 | 用途 |
| --- | --- |
| `/` | 首页，展示简介、精选项目和最新文章 |
| `/projects` | 项目列表 |
| `/projects/[slug]` | 项目详情 |
| `/articles` | 文章列表与搜索 |
| `/articles/[slug]` | 文章详情 |
| `/about` | 个人介绍 |
| `/resume` | 简历 |
| `/feed.xml` | RSS 订阅 |
| `/sitemap.xml` | 搜索引擎站点地图 |
| `/robots.txt` | 爬虫规则 |

## 4. 发布或修改文章

文章位于 `src/content/posts/`。文件名就是 URL slug，必须使用小写英文字母、数字和连字符，例如 `my-new-post.md`。

新建文章时可使用以下模板：

```yaml
---
title: "文章标题"
description: "用于列表和搜索结果的简短摘要"
date: "2026-08-20"
updated: "2026-08-20" # 可选
category: "Engineering" # Engineering | Research | Learning | Thoughts
tags: ["Next.js", "TypeScript"]
published: true
featured: false
cover: null # 可选，站内绝对路径或 http/https URL
canonicalUrl: null # 可选，转载内容的原始 URL
---
```

使用时注意：

- `description` 长度应为 10–240 个字符。
- `date` 和 `updated` 必须使用 `YYYY-MM-DD` 格式。
- `published: false` 的文章不会出现在公开列表或生成详情页。
- `featured: true` 会提高文章在首页精选逻辑中的优先级。
- Markdown 图片必须填写有意义的 alt 文本。
- 文章支持目录、代码高亮与复制、KaTeX 公式和 Mermaid 图表。

## 5. 发布或修改项目

项目位于 `src/content/projects/`，文件名同样使用小写英文 slug。

新建项目时可使用以下模板：

```yaml
---
title: "项目名称"
description: "说明项目解决的问题"
date: "2026-08-20"
updated: "2026-08-20" # 可选
role: "Full-stack Developer — 负责架构与实现"
tech_stack: ["TypeScript", "Next.js"]
github_url: "https://github.com/owner/repo"
demo_url: null
status: "进行中" # 草稿 | 进行中 | 已完成
featured: true
cover: null
---
```

使用时注意：

- `role` 应写清个人职责，不要只写模糊的团队贡献。
- `tech_stack` 至少包含一项技术。
- `github_url` 和 `demo_url` 只能使用 `http` 或 `https` URL，不存在时填写 `null` 或省略。
- `status: "草稿"` 的项目不会生成公开页面。
- 项目描述、性能数据和个人贡献必须使用可验证的事实。

## 6. 图片与静态资源

静态资源放在 `public/` 目录。在 Markdown 或页面中优先使用站内绝对路径：

```markdown
![图片内容说明](/images/example.png)
```

不要引用本机绝对路径、临时文件或仓库中不存在的资源。使用外链图片时应确保 URL 可长期访问且采用 HTTPS。

## 7. 主题与访问

顶部导航的主题选择器支持暖色、星空、浅色、灰色和深色主题，选择会保存在当前浏览器中。站点动效会遵循系统的“减少动态效果”设置。

桌面端导航直接展示主要页面；移动端通过右上角菜单打开，并支持 `Esc` 键关闭。

## 8. 提交与发布

推荐按以下顺序更新站点：

1. 从最新 `main` 创建工作分支。
2. 修改内容或代码。
3. 执行 `npm.cmd run content:validate`。
4. 执行 `npm.cmd run check`。
5. 推送工作分支，在 Vercel Preview 检查实际页面。
6. 合并到 `main`，由 Vercel 发布生产站点。
7. 打开主域名完成生产验收。

不要把“本地构建通过”当成“线上发布完成”。提交、推送、Vercel Preview、合并和生产验证是不同阶段。

### 内容快速发布脚本

WSL 或 Bash 环境可使用：

```bash
./publish.sh "新增文章：标题"
```

该脚本只执行内容校验，并且只提交 `src/content/` 下的变更，随后推送当前分支。它不会提交代码或文档，也不能代替 `npm.cmd run check` 和 Vercel Preview 验收。

## 9. 发布前检查清单

- `npm.cmd run check` 完整通过。
- 首页、文章、项目、关于和简历页能够正常打开。
- 草稿文章和草稿项目没有公开。
- 文章目录、代码复制、公式和 Mermaid 图表正常。
- 桌面端和移动端导航正常，主题可切换。
- 图片能显示且 alt 文本正确。
- `/feed.xml`、`/sitemap.xml` 和 `/robots.txt` 能够访问。
- canonical、Open Graph 分享图和 JSON-LD 中的主域名一致。
- `https://starrylovetbao.cloud` 正确跳转到 `https://www.starrylovetbao.cloud`，且没有重定向循环。

## 10. 域名与 SEO 配置

主域名定义在 `src/lib/site.ts` 的 `SITE_URL`。如果以后更换域名，必须同时核对：

- sitemap 和 robots。
- 页面 canonical。
- Open Graph 分享图。
- JSON-LD 结构化数据。
- Vercel 项目中的域名与重定向设置。

不要在 Next.js 和 Vercel 中同时配置相反方向的域名跳转，否则会形成重定向循环。

## 11. 常见问题

### 内容校验失败

根据错误信息检查 frontmatter 字段、日期格式、slug、URL 协议和 Markdown 图片 alt。本项目使用严格 schema，额外或拼错的 frontmatter 字段也会被拒绝。

### 页面没有显示新内容

检查文章的 `published` 是否为 `true`，或项目的 `status` 是否仍为 `草稿`。如果是部署后未更新，再确认相应提交已经推送、合并，且 Vercel 生产部署成功。

### 线上出现重定向循环

同时检查 Next.js 配置和 Vercel 域名规则，确保只保留单一、一致的跳转方向。
