# Starry Blog 使用手册

## 写作与发布

本站以 GitHub 中的 Markdown 为唯一内容源，不提供网页管理后台。

1. 在 `src/content/posts/` 新建文章，文件名即 URL slug。
2. 按 README 的 frontmatter 契约填写元数据。
3. 运行 `npm run content:validate` 和 `npm run dev`。
4. 推送分支，在 Vercel Preview 检查桌面端、移动端和分享信息。
5. 合并到 `main` 后自动发布。

项目位于 `src/content/projects/`，成长记录位于
`src/content/timeline.json`。分类、状态、日期、URL 与图片 alt
会在构建阶段校验。

## 常用命令

```bash
npm run dev               # 本地开发
npm run content:validate  # 内容校验
npm run lint              # ESLint
npm run typecheck         # TypeScript
npm test                  # 单元测试
npm run build             # 生产构建
npm run check             # 按 CI 顺序执行全部检查
```

## 主题

顶部导航的主题选择器支持暖色、星空、浅色、灰色和深色主题，选择会
保存在浏览器中。所有动效均遵循系统的“减少动态效果”设置。

## 发布前检查

- 项目描述只使用可验证事实，不补写未经验证的指标。
- 新文章至少包含清晰摘要、分类、标签和完整正文。
- Markdown 图片必须提供有意义的 alt 文本。
- 检查文章目录、代码复制、公式和 Mermaid 图表。
- 检查 `/feed.xml`、分享图、canonical 和移动端菜单。
